# trilium-server-deploy

## Requirement:
1. Install docker

## Run
```
sudo docker run -t -i -p 127.0.0.1:8080:8080 -v ~/trilium-data:/root/trilium-data zadam/trilium:[VERSION]
```

Reference:
 1. [Docker server installation]([https://github.com/zadam/trilium/wiki/Docker-server-installation)
